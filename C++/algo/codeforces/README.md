# Codeforces

Each subfolder is a **contest id** (round or gym id used locally). Inside you will find `A.cpp`, `B.cpp`, … plus optional `input.txt` for local runs.

Use `template.cpp` in this directory as a starting point for new files.
