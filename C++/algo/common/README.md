# `common/`

Portable algorithm snippets and toys intended for copy-paste or linking from contest solutions.

Subtopics: `data_structure/`, `dp/`, `graph/`, `math/`, `strings/`. Some folders include their own `CMakeLists.txt` for local tests.
