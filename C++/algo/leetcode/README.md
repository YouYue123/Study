# LeetCode

Solutions are single-file C++ sources named `<problem_id>.cpp` (LeetCode problem number).

Many solutions define `class Solution` with the method required by the platform. Build or paste into LeetCode’s runner as needed.
